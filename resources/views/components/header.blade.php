<div class="card-header">
    <div style="display: flex; justify-content: space-between; align-items: center;">
        <h1 class="h3">{{ $name }}</h1>
        <a class="btn btn-{{ $classBtn }}" href="{{ route($route) }}">{{ $buttonText }}</a>
    </div>
</div>
